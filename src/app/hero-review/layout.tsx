/**
 * Hero Review — Isolated layout (no site shell, no nav, no footer).
 * Used for internal pre-production hero evaluation only.
 */
export default function HeroReviewLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
